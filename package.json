{
  "name": "bytesmith",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fast, private, in-browser developer tools for JSON, strings, and diffs.",
  "scripts": {
    "dev": "pnpm --filter @bytesmith/web dev",
    "build": "pnpm --filter @bytesmith/web build",
    "test": "pnpm --filter @bytesmith/core test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@11.7.0"
}
